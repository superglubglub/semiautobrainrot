# semiautomaticbrainrot

## usage
from project directory, run modules/sab.py

you can replace the example url in main to generate a different post.
max length per video is ~5m before things start to go wrong.